# 화면별 TC & 핵심 시나리오 (Test Cases & Scenarios)

## 0. 개요
이 문서는 앱의 각 화면별 테스트 케이스(TC)와 핵심 사용자 경험(UseCase)을 검증하기 위한 시나리오를 정의합니다.

---

## 1. 화면별 TC (Screen Test Cases)

### 1.1 스플래시 및 로그인 (Splash & Login)
**화면**: `SplashScreen`, `EmailLoginScreen`

| ID | 케이스명 | 사전 조건 | 액션 | 기대 결과 | 비고 |
|:---:|:---|:---|:---|:---|:---|
| S-01 | **자동 로그인** | 이전에 로그인한 기록 있음 | 앱 실행 | `HomeScreen`으로 즉시 진입 | |
| S-02 | **게스트 로그인** | 최초 실행 또는 로그아웃 상태 | '둘러보기' 버튼 탭 | 게스트 계정 생성 후 `HomeScreen` 이동 | 상단에 '게스트' 표시 확인 |
| S-03 | **소셜 로그인 (Google)** | 로그아웃 상태 | 구글 로그인 버튼 탭 | 구글 계정 인증 후 계정 생성/로그인 및 `HomeScreen` 이동 | |
| S-04 | **이메일 로그인 진입** | 로그아웃 상태 | '이메일로 계속하기' 탭 | `EmailLoginScreen`으로 이동 | |
| L-01 | **유효하지 않은 이메일** | `EmailLoginScreen` | 잘못된 이메일 형식 입력 | 에러 메시지 노출 (유효한 이메일을 입력해주세요) | |
| L-02 | **로그인 성공** | `EmailLoginScreen` | 유효한 이메일/비밀번호 입력 후 버튼 탭 | 로그인 성공 후 `HomeScreen` 이동 | |

### 1.2 연결 화면 (Connect)
**화면**: `ConnectScreen` (로그인 후 파트너 미연결 시 접근, 또는 설정에서 접근)

| ID | 케이스명 | 사전 조건 | 액션 | 기대 결과 | 비고 |
|:---:|:---|:---|:---|:---|:---|
| C-01 | **초대 코드 생성** | 미연결 유저 | '초대 코드 생성' 버튼 탭 | 6자리 또는 8자리 초대 코드 표시 & 공유하기 시트 노출 | |
| C-02 | **초대 코드 입력 (유효)** | 미연결 유저 | 상대방의 유효한 코드 입력 | 연결 성공 팝업 -> `HomeScreen` 이동 | 상대방 프로필 정보 표시 |
| C-03 | **초대 코드 입력 (오류)** | 미연결 유저 | 잘못된/만료된 코드 입력 | 에러 토스트 메시지 노출 | |

### 1.3 홈 - Now 탭 (Now Tab)
**화면**: `HomeScreen` > `NowTab`

| ID | 케이스명 | 사전 조건 | 액션 | 기대 결과 | 비고 |
|:---:|:---|:---|:---|:---|:---|
| N-01 | **카드 없음 (계획 필요)** | 활성 계획 없음 | 화면 진입 | Plan Needed(Type 2-1) 카드 노출 & '계획 만들기' 버튼 표시 | |
| N-02 | **지금 실천 (Now Action)** | 현재 시간대 실천 계획 있음 | '했어' 버튼 탭 | 완료 애니메이션 동작, DB 업데이트, 카드가 사라지거나 상태 변경 | |
| N-03 | **오늘 완료 (Today Done)** | 오늘 모든 할 일 완료 | 화면 확인 | "오늘 다 했어요"(Type 2-2) 카드 노출 | |
| N-04 | **파트너 소식 확인** | 파트너가 실천함 | 화면 상단/중앙 확인 | Partner Action Share(Type 4) 카드 노출, 프로필 이미지 확인 | |
| N-05 | **파트너 소식 반응** | Partner Action Share 카드 있음 | '확인하기' 버튼 탭 | "함께하는 중" 등으로 상태 변경, 버튼 비활성화 또는 카드 변경 | |
| N-06 | **디버그 시나리오 전환** | Mock 모드 빌드 | 우측 하단 🐞 FAB 탭 | 시나리오 선택 다이얼로그 노출 및 선택 시 UI 즉시 반영 | |

### 1.4 홈 - Our 탭 (Us Tab)
**화면**: `HomeScreen` > `UsTab`

| ID | 케이스명 | 사전 조건 | 액션 | 기대 결과 | 비고 |
|:---:|:---|:---|:---|:---|:---|
| U-01 | **계획 목록 조회** | 연결된 계획 존재 | 탭 진입 | 현재 진행 중인 우리/나/너의 계획 리스트 표시 | |
| U-02 | **새 계획 추가 진입** | - | (+) 버튼 탭 | `PlanCreateScreen`으로 이동 | |

### 1.5 기록 탭 (Record Tab / History)
**화면**: `HomeScreen` > `RecordTab`

| ID | 케이스명 | 사전 조건 | 액션 | 기대 결과 | 비고 |
|:---:|:---|:---|:---|:---|:---|
| R-01 | **히스토리 조회** | 과거 기록 존재 | 탭 진입 | 날짜별/항목별 히스토리 리스트 표시 | |
| R-02 | **지난 기록 소명(Reconcile)** | 미완료(Skipped/Missed) 기록 있음 | 항목 탭 또는 메뉴 탭 | '뒤늦게 완료' / '쉬었음' / '넘어가기' 선택 팝업 노출 | |

### 1.6 계획 생성 (Plan Create)
**화면**: `PlanCreateScreen`

| ID | 케이스명 | 사전 조건 | 액션 | 기대 결과 | 비고 |
|:---:|:---|:---|:---|:---|:---|
| P-01 | **단계 이동** | - | '다음' 버튼 탭 | 다음 단계(제목 -> 시간 -> 요일 등)로 슬라이드 이동 | |
| P-02 | **필수 입력 체크** | - | 입력 없이 '다음' 탭 | 버튼 비활성화 상태 또는 유효성 검사 에러 | |
| P-03 | **계획 생성 완료** | 마지막 단계 | '완료' 버튼 탭 | 로딩 후 `NowTab`으로 이동, 새 계획 카드 표시 | |

### 1.7 설정 (Settings)
**화면**: `SettingsScreen`

| ID | 케이스명 | 사전 조건 | 액션 | 기대 결과 | 비고 |
|:---:|:---|:---|:---|:---|:---|
| ST-01 | **언어 변경** | 한국어 상태 | 영어 선택 | 앱 전체 언어가 영어로 변경됨 | |
| ST-02 | **로그아웃** | 로그인 상태 | 로그아웃 탭 | 로그인 화면 또는 스플래시로 이동 | |
| ST-03 | **회원 탈퇴** | - | 탈퇴하기 탭 | 재확인 팝업 -> 탈퇴 처리 및 초기화면 이동 | |

---

## 2. 중요 UseCase 별 시나리오 (Key Scenarios)

### UC-1: [Core Loop] 계획 생성부터 첫 실천까지
1. **계획 생성**: `UsTab` -> (+) 버튼 -> "매일 저녁 8시 독서" 입력 -> 완료.
2. **상태 확인**: `NowTab` 진입. 현재 시간이 8시 근처라면 **Now Action(Type 1)** 카드 확인.
   - 시간이 아니라면 **Vague Time(점심쯤 등)** 표시 확인.
3. **실천**: "했어" 버튼 클릭.
4. **결과 확인**:
   - `NowTab`에서 카드가 사라지거나 **Today Done(Type 2-2)** 상태로 변경됨.
   - `RecordTab`에 "독서 - 완료" 기록 추가 확인.

### UC-2: [Interaction] 파트너와 함께하기
1. **연결**: `ConnectScreen` -> 초대 코드 교환 -> 연결 완료.
2. **파트너 활동 감지**:
   - [시나리오 툴 사용]: `partnerActionShare` 선택.
   - `NowTab`에 **"지민님이 독서를 완료했어요!"(Type 4)** 카드 등장 확인.
3. **반응하기**: "확인하기" 버튼 클릭.
   - 카드가 **"함께하는 중"** 상태로 바뀌거나, 스낵바 피드백("지민님에게 응원을 보냈어요") 출력.

### UC-3: [Edge Case] 바쁜 날 챙기기 (Overdue & Reconcile)
1. **상황**: 어제 "영양제 먹기"를 못 누름.
2. **진입**: `NowTab` 확인.
3. **지난 실천 카드**: **Overdue Self Action(Type 5)** "영양제, 늦게라도 챙길까요?" 카드 확인.
4. **처리**:
   - 옵션 A: "했어" -> 어제 날짜(또는 지난 시간)로 완료 처리.
   - 옵션 B: "오늘은 넘어가자" -> 스킵(Skipped) 처리.
5. **확인**: 카드가 사라지고 `relaxedDay` 또는 다음 할 일 카드가 나옴.

### UC-4: [Onboarding] 게스트 -> 정회원 전환
1. **게스트 사용**: 게스트 로그인으로 며칠간 앱 사용 및 기록 축적.
2. **가입 유도**: 설정 -> "계정 연결/저장하기" 또는 `ConnectScreen` 진입 시 로그인 유도.
3. **소셜 로그인**: 구글 로그인 수행.
4. **데이터 유지**: 게스트 시절의 기록이 사라지지 않고 구글 계정에 연동되는지 확인.
