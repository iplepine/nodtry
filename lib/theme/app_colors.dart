import 'package:flutter/material.dart';

/// OnMyBehalf Warm Accountability Design System Colors
class AppColors {
  AppColors._();

  // Primary Color - 신뢰 & 응원
  static const Color warmCoral = Color(0xFFF28B82);

  // Secondary Color - 안정 & 지속
  static const Color softSageGreen = Color(0xFF8FAE9E);

  // Background Colors
  static const Color warmOffWhite = Color(0xFFFAF8F6);
  static const Color lightSand = Color(0xFFF2ECE7);

  // Text Colors
  static const Color primaryText = Color(0xFF3F3A36);
  static const Color secondaryText = Color(0xFF7A726C);

  // Neutral / Divider
  static const Color softGray = Color(0xFFD8D3CF);

  // Status Colors
  static const Color neutralGray = Color(0xFFCFCAC6); // 미보고
  static const Color lightCoral = Color(0xFFF6B7B1); // 보고됨 (대기)
}

